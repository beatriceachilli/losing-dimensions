name: latent_dim_diff
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
  
dependencies:
  # Core Environment
  - python=3.9
  
  - pytorch=2.1.2
  - torchvision=0.16.2
  - torchaudio=2.1.2
  - pytorch-cuda=11.8
  
  - numpy=1.24.2
  - scipy=1.10.1
  - pillow=10.2.0
  - matplotlib=3.9.1
  - tqdm=4.65.0
  
  # specific pip packages
  - pip
  - pip:
      - diffusers==0.26.3
      - piqa==1.3.2
      - simple_parsing==0.1.5
      - einops==0.7.0